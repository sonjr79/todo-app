# Todo App Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 브라우저에서 동작하는 단일 HTML 파일 Todo 앱 (localStorage 저장)

**Architecture:** 단일 `index.html`에 HTML/CSS/JS 모두 포함. localStorage에 JSON 배열로 할 일 목록 저장. 외부 라이브러리 없음.

**Tech Stack:** HTML5, CSS3, Vanilla JavaScript, localStorage

---

## File Structure

- Create: `index.html` — 전체 앱 (마크업 + 스타일 + 로직)

---

### Task 1: HTML 구조 생성

**Files:**
- Create: `index.html`

- [ ] **Step 1: 기본 HTML 골격 작성**

```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Todo</title>
</head>
<body>
  <div id="app">
    <h1>Todo</h1>
    <div id="input-area">
      <input type="text" id="todo-input" placeholder="할 일을 입력하세요" />
      <button id="add-btn">추가</button>
    </div>
    <ul id="todo-list"></ul>
  </div>
  <style></style>
  <script></script>
</body>
</html>
```

- [ ] **Step 2: 브라우저에서 파일 열어 구조 확인**

`index.html`을 브라우저로 열어 입력창과 버튼이 보이는지 확인.

- [ ] **Step 3: Commit**

```bash
git init
git add index.html
git commit -m "feat: add html structure"
```

---

### Task 2: CSS 스타일 추가

**Files:**
- Modify: `index.html` (style 태그 내부)

- [ ] **Step 1: style 태그 안에 CSS 작성**

```css
* { box-sizing: border-box; margin: 0; padding: 0; }
body { font-family: sans-serif; background: #f5f5f5; display: flex; justify-content: center; padding: 40px 16px; }
#app { background: white; border-radius: 8px; padding: 24px; width: 100%; max-width: 480px; box-shadow: 0 2px 8px rgba(0,0,0,.1); }
h1 { font-size: 1.5rem; margin-bottom: 16px; }
#input-area { display: flex; gap: 8px; margin-bottom: 16px; }
#todo-input { flex: 1; padding: 8px 12px; border: 1px solid #ddd; border-radius: 4px; font-size: 1rem; }
#add-btn { padding: 8px 16px; background: #4f46e5; color: white; border: none; border-radius: 4px; cursor: pointer; font-size: 1rem; }
#add-btn:hover { background: #4338ca; }
#todo-list { list-style: none; }
.todo-item { display: flex; align-items: center; gap: 10px; padding: 10px 0; border-bottom: 1px solid #f0f0f0; }
.todo-item:last-child { border-bottom: none; }
.todo-item input[type="checkbox"] { width: 18px; height: 18px; cursor: pointer; }
.todo-item .text { flex: 1; font-size: 1rem; }
.todo-item.done .text { text-decoration: line-through; color: #aaa; }
.todo-item .del-btn { background: none; border: none; color: #ccc; cursor: pointer; font-size: 1.1rem; }
.todo-item .del-btn:hover { color: #e53e3e; }
```

- [ ] **Step 2: 브라우저 새로고침 후 스타일 확인**

카드 레이아웃과 보라색 추가 버튼이 보이면 정상.

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "feat: add styles"
```

---

### Task 3: JavaScript 로직 추가

**Files:**
- Modify: `index.html` (script 태그 내부)

- [ ] **Step 1: script 태그 안에 JS 작성**

```js
const input = document.getElementById('todo-input');
const addBtn = document.getElementById('add-btn');
const list = document.getElementById('todo-list');

let todos = JSON.parse(localStorage.getItem('todos') || '[]');

function save() {
  localStorage.setItem('todos', JSON.stringify(todos));
}

function render() {
  list.innerHTML = '';
  todos.forEach((todo, i) => {
    const li = document.createElement('li');
    li.className = 'todo-item' + (todo.done ? ' done' : '');

    const cb = document.createElement('input');
    cb.type = 'checkbox';
    cb.checked = todo.done;
    cb.addEventListener('change', () => {
      todos[i].done = cb.checked;
      save();
      render();
    });

    const span = document.createElement('span');
    span.className = 'text';
    span.textContent = todo.text;

    const del = document.createElement('button');
    del.className = 'del-btn';
    del.textContent = '✕';
    del.addEventListener('click', () => {
      todos.splice(i, 1);
      save();
      render();
    });

    li.appendChild(cb);
    li.appendChild(span);
    li.appendChild(del);
    list.appendChild(li);
  });
}

function addTodo() {
  const text = input.value.trim();
  if (!text) return;
  todos.push({ id: Date.now(), text, done: false });
  input.value = '';
  save();
  render();
}

addBtn.addEventListener('click', addTodo);
input.addEventListener('keydown', e => { if (e.key === 'Enter') addTodo(); });

render();
```

- [ ] **Step 2: 브라우저에서 기능 검증**

  1. 할 일 입력 후 추가 버튼 클릭 → 목록에 나타남
  2. Enter 키로 추가 → 동작 확인
  3. 체크박스 클릭 → 취소선 표시
  4. ✕ 버튼 클릭 → 항목 삭제
  5. 페이지 새로고침 → 목록 유지됨

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "feat: add todo logic with localStorage"
```
